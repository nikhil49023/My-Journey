 These values determine the **importance or strength** of each corresponding input (10:55). Like inputs, they are stored in a list (e.g., `weights = [0.2, 0.8, -0.5]`) 
 
 A higher weight value indicates that the corresponding input has a stronger influence on the neuron's final output