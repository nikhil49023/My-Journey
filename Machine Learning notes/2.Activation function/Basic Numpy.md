np.sum(arr) : used to find sum of all elements in an array
np.sum(arr, axis = 0) : used to find sum of all elements in rows of the array
np.sum(arr, axis = 1) : used to find sum of all elements in column of the array
