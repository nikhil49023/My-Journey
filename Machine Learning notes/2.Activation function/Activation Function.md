Used in Dense Layers

An activation function is a crucial mathematical function that determines whether a neuron should be "activated" or not in a neural network

In activation function is a crucial mathematical function that determines whether a neuron should be "activated" or not in a neural network

Without this (**activation function**), no matter how many Dense layers you stack, the whole network would just behave like one single, giant linear equation.

This would make it impossible for the model to learn complex, curvy patterns in data—like recognizing the shape of a digit or the sentiment of a sentence.